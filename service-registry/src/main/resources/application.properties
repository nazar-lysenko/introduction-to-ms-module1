spring.application.name=service-registry
server.port=${SERVICE_REGISTRY_PORT:8761}

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.server.wait-time-in-ms-when-sync-empty=${SERVICE_REGISTRY_EMPTY_SYNC_WAIT_MS:0}
